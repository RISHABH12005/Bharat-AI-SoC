## System Dependencies (Raspberry Pi)

Install these before creating the Python virtual environment:

```bash
sudo apt update
sudo apt install -y \
  python3 \
  python3-venv \
  python3-dev \
  portaudio19-dev \
  alsa-utils \
  espeak-ng

```
